ENV=development

# Postgres de Supabase (Project Settings > Database > Connection string)
DATABASE_URL=postgresql://postgres:password@host:5432/postgres

# Project Settings > API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Dify Cloud: crea una app "Chatflow/Agent" (clasificador) y otra (asistente),
# copia la API key de cada una en Dify > tu app > API Access
DIFY_API_KEY=
DIFY_CHAT_API_KEY=
DIFY_BASE_URL=https://api.dify.ai/v1

# Microsoft Graph (Outlook). Vacío = integración deshabilitada (modo demo).
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_MAILBOX=

# Rutas locales a los artefactos de ML (no versionar el .pkl real en git si es privado)
ML_MODEL_PATH=./ml/modelo_riesgo_cliente_traza.pkl
ML_FEATURES_PATH=./ml/features_riesgo_cliente_traza.pkl
SEED_DATA_DIR=./data/csv

FRONTEND_URL=http://localhost:5173
